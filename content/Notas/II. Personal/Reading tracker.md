---
title: "Reading tracker"
layout: "reading-tracker"
BookToC: false
bookCollapseSection: true
---

Edit `Current`, `Start`, and `End` directly in the table.

After editing, use **Copy Overrides YAML** or **Download Overrides YAML**, then update `data/reading_tracker_overrides.yaml` and commit.
